# Structured micro-lesson prompt

Return JSON matching the lesson schema. Use only registered concepts, misconceptions and animation templates. Do not emit HTML, scripts, URLs or executable JavaScript. Include one age-appropriate example and one quiz with exactly one correct answer. Cite the supplied source chunk identifiers.
